---
- name: Configure Fedora Kinoite Desktop
  hosts: localhost
  connection: local
  gather_facts: yes
  vars:
    ansible_python_interpreter: /usr/bin/python3

  roles:
    - common
    - system_config
    - package_config
    - shell_config
    - apps_config
    - kde_theme
    - kde_config
